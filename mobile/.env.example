# Point at your machine's LAN IP when testing on a physical device or the
# iOS/Android simulator -- "localhost" there refers to the device itself, not
# your dev machine. Use the deployed Render URL for production builds.
EXPO_PUBLIC_API_URL=http://192.168.1.23:3001
